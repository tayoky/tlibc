#!/bin/sh

. ./tconf.sh

# defaults
: ${STATIC:=yes}
: ${SHARED:=no}
: ${SYSROOT=./sysroot}

OPTIONS="STATIC SHARED"

tconf_init "$@"

tconf_search_cc "$HOST"
tconf_search_as "$HOST"
tconf_search_ar "$HOST"
tconf_find_os

tconf_check_attribute "$CC" "weak"

ARCH=${HOST%%-*}

tconf_fini
